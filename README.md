# Circle Raider — модульная версия на Godot

Изометрический рогалик для Godot 4.7. Браузерная версия сохранена в ветке `circle-raider`, а эта версия находится в ветке `godot-port`.

В проекте нет обязательных внешних изображений: герой, оружие, противники, предметы и локация рисуются средствами Godot из кода. Благодаря этому каждую механику можно менять независимо.

## Быстрый запуск

1. Запустите Godot 4.7.
2. В менеджере проектов нажмите **Import**.
3. Выберите `project.godot` из корня репозитория.
4. Откройте проект.
5. Нажмите `F6` или кнопку запуска проекта.

Управление:

- `WASD` — движение;
- мышь — направление взгляда и оружия;
- левая кнопка мыши — удар мечом.

## Как устроен игровой цикл

```text
project.godot
└── scenes/main.tscn
    └── scripts/main.gd
        ├── создаёт локацию и героя
        ├── принимает команды WaveManager
        ├── создаёт нужную сцену противника
        ├── создаёт снаряды, опыт и бутылки
        └── передаёт данные здоровья, опыта и волн в GameUI
```

Герой хранит игровые координаты в `world_position`. Файл `iso_math.gd` переводит их в экранные изометрические координаты. Поэтому логика движения и дистанций остаётся простой двухмерной, а изображение выглядит изометрическим.

## Быстрый указатель: что менять

| Нужно изменить | Файл | Параметр или функция |
|---|---|---|
| Скорость героя | `scripts/player/player_movement.gd` | `speed` |
| Плавность разгона | `scripts/player/player_movement.gd` | `acceleration` |
| Плавность остановки | `scripts/player/player_movement.gd` | `deceleration` |
| Начальное здоровье | `scripts/player/player.gd` | `max_health`, `health` |
| Размер героя | `scripts/player/player.gd` | `radius` |
| Урон меча | `scripts/player/player_attack.gd` | `damage` |
| Длина меча | `scripts/player/player_attack.gd` | `sword_length` |
| Ширина размаха | `scripts/player/player_attack.gd` | `SWEEP_START`, `SWEEP_END` |
| Частота ударов | `scripts/player/player_attack.gd` | `cooldown_duration` |
| Скорость анимации удара | `scripts/player/player_attack.gd` | `swing_duration` |
| Внешний вид героя и меча | `scripts/player/player.gd` | `_draw()`, `_draw_sword()` |
| Параметры конкретного врага | соответствующий файл в `scripts/enemies/` | `_init()` |
| Поведение конкретного врага | соответствующий файл в `scripts/enemies/` | `tick_behavior()` |
| Внешность конкретного врага | соответствующий файл в `scripts/enemies/` | `_draw()` |
| Серый боец и его кулаки | `scripts/enemies/brawler_enemy.gd` | `_init()`, `tick_behavior()`, `_draw()` |
| Скорость и модель стрелы | `scripts/combat/enemy_projectile.gd` | `setup()`, `_draw()` |
| Длительность волны | `scripts/systems/wave_manager.gd` | `wave_duration` |
| Частота появления врагов | `scripts/systems/wave_manager.gd` | расчёт `spawn_time` |
| Состав волн | `scripts/systems/wave_manager.gd` | `_choose_enemy()` |
| Размер карты | `scripts/world/location.gd` и `scripts/player/player.gd` | `world_limit` |
| Размер плитки пола | `scripts/world/location.gd` | `tile_size` |
| Количество кустов, бочек и деревьев | `scripts/world/location.gd` | вызовы `_generate_kind()` |
| Шанс выпадения лечения | `scripts/world/world_prop.gd` | `potion_chance` |
| Сила лечения | `scripts/main.gd` | `_spawn_potion()`, значение `28` |
| Радиус магнита опыта | `scripts/items/pickup.gd` | `player.attack.sword_length + 42.0` |
| Интерфейс и тексты | `scripts/ui/game_ui.gd` | методы `_build_*()` |

## Корневые файлы

### `project.godot`

Главная конфигурация Godot.

Что здесь находится:

- имя проекта — `config/name`;
- стартовая сцена — `run/main_scene`;
- разрешение — параметры `window/size/*`;
- действия управления — раздел `[input]`;
- названия физических слоёв — `[layer_names]`;
- режим рендера — `[rendering]`.

Для переназначения клавиш удобнее открыть **Project → Project Settings → Input Map**, а не редактировать сериализованные `InputEvent` вручную.

### `.gitignore`

Исключает из Git локальный кэш Godot `.godot/`, папки сборок, настройки редакторов и вложенный тестовый проект `warrioir-/`.

Файлы исходного кода и `.gd.uid` игнорировать не нужно.

### `README.md`

Этот документ. Его изменение не влияет на игру.

## Сцены

Сцены хранят узлы Godot и подключают соответствующие скрипты.

### `scenes/main.tscn`

Стартовая сцена. Содержит корневой `Node2D` со скриптом `scripts/main.gd`.

Обычно добавлять игровые объекты вручную сюда не нужно: главный скрипт создаёт их при запуске. Сюда можно добавить постоянные узлы, например аудиоплеер, систему частиц или общий постпроцессинг.

### `scenes/player/player.tscn`

Отдельная сцена героя. Подключает `scripts/player/player.gd`.

Параметр `world_limit` доступен в Inspector. Модули `Movement`, `Attack` и `Camera` создаются в `_ready()` скрипта героя, поэтому их стартовые числовые значения сейчас меняются в соответствующих `.gd`-файлах.

### `scenes/world/location.tscn`

Сцена локации. Подключает `scripts/world/location.gd`.

Через Inspector можно менять:

- `world_limit` — половина ширины и высоты мира;
- `tile_size` — размер одной плитки изометрического пола.

### `scenes/enemies/brawler_enemy.tscn`

Сцена базового серого бойца с двумя кулаками. Подключает `brawler_enemy.gd`.

### `scenes/enemies/melee_enemy.tscn`

Сцена зелёного мечника. Подключает `melee_enemy.gd`.

### `scenes/enemies/shooter_enemy.tscn`

Сцена красного стрелка. Подключает `shooter_enemy.gd`.

### `scenes/enemies/lancer_enemy.tscn`

Сцена ромба-копейщика. Подключает `lancer_enemy.gd`.

### `scenes/enemies/boss_enemy.tscn`

Сцена большого босса с топором. Подключает `boss_enemy.gd`.

Параметры врагов пока задаются в `_init()` их скриптов. Чтобы редактировать их через Inspector, можно заменить нужную переменную в `enemy_base.gd` или дочернем скрипте на `@export var` и сохранить значение в сцене.

## Главная сборка

### `scripts/main.gd`

Связывает все игровые системы. Здесь нет подробной логики отдельного врага или меча — только создание объектов и передача событий.

Основные части:

- константы `*_SCENE` загружают сцены игрока, локации и противников;
- `_ready()` создаёт системы и соединяет сигналы;
- `_start_run()` начинает новый забег;
- `_clear_runtime_nodes()` удаляет старых врагов, снаряды и предметы при перезапуске;
- `_spawn_enemy()` выбирает сцену по строке `enemy_kind`;
- `_spawn_projectile()` создаёт снаряд стрелка;
- `_spawn_potion()` создаёт бутылку лечения;
- `_spawn_experience()` делит опыт на несколько магнитящихся сфер;
- `_on_enemy_died()` выдаёт опыт и распознаёт смерть босса;
- `_start_boss()` убирает обычных врагов после седьмой волны;
- `_apply_upgrade()` применяет выбранное усиление.

Часто изменяемые значения:

- `620.0` в `_spawn_enemy()` — дистанция появления обычного врага от героя;
- `520.0` — дистанция появления босса;
- `3450.0` — ограничение точки появления внутри карты;
- `28` в `_spawn_potion()` — количество восстанавливаемого здоровья;
- `10` в `_spawn_experience()` — максимальная ценность одной сферы опыта.

Чтобы зарегистрировать новый тип врага, нужно добавить его сцену в константы сверху и новую ветку в `match enemy_kind` внутри `_spawn_enemy()`.

## Изометрия

### `scripts/core/iso_math.gd`

Содержит только преобразование координат.

- `X_SCALE = 1.18` отвечает за горизонтальное растяжение ромбической сетки;
- `Y_SCALE = 0.56` отвечает за вертикальное сжатие;
- `world_to_screen()` переводит координаты мира в позицию на экране;
- `screen_to_world()` выполняет обратное преобразование;
- `world_direction_from_screen()` переводит направление `WASD` или мыши в направление мира;
- `screen_angle()` вычисляет угол для отрисовки оружия.

Если изменить `X_SCALE` или `Y_SCALE`, изменится наклон всей изометрии. Эти значения должны применяться ко всем объектам через данный файл; не дублируйте формулу в других скриптах.

## Герой

### `scripts/player/player.gd`

Хранит состояние героя и рисует его модель.

Основные переменные:

- `world_limit = 3600.0` — предел перемещения;
- `radius = 22.0` — стартовый размер круга;
- `max_health = 100.0` — максимальное здоровье;
- `health = 100.0` — текущее здоровье;
- `experience_required = 100` — опыт для первого повышения уровня;
- `invulnerability` — оставшееся время неуязвимости после получения урона.

Важные методы:

- `_process()` обновляет направление мыши, движение и ввод атаки;
- `_update_aim()` рассчитывает направление взгляда относительно центра экрана;
- `take_damage()` уменьшает здоровье и включает `0.45` секунды неуязвимости;
- `heal()` лечит героя;
- `add_experience()` повышает уровень и пересчитывает следующий порог по формуле `experience_required * 1.24 + 18`;
- `upgrade_speed()` увеличивает скорость на `12%`;
- `upgrade_vitality()` добавляет `25` здоровья и увеличивает радиус на `2.5`;
- `reset_run()` возвращает стартовые параметры;
- `_draw()` рисует тень, тело, контур, глаз, меч и след размаха;
- `_draw_swing_trail()` строит след клинка и поддерживает оба направления замаха;
- `_draw_sword()` строит модель клинка из линий и полигона.

Важно: если меняете стартовое здоровье или размер, обновите те же значения внутри `reset_run()`, иначе после перезапуска забега вернутся старые числа.

### `scripts/player/player_movement.gd`

Отвечает только за движение.

Экспортируемые параметры:

- `speed = 195.0` — максимальная скорость;
- `acceleration = 1250.0` — скорость разгона;
- `deceleration = 1550.0` — скорость остановки.

`step()` считывает `WASD`, переводит экранное направление в координаты мира, плавно изменяет `velocity` и ограничивает позицию границами карты.

Примеры:

- более тяжёлый герой: уменьшить `acceleration` до `700`, `deceleration` до `900`;
- более отзывчивое движение: увеличить оба значения до `2200`;
- ускорить героя: увеличить `speed`.

Если меняете стартовую `speed`, обновите значение `195.0` в `PlayerHero.reset_run()`.

### `scripts/player/player_attack.gd`

Отвечает только за атаку и попадания.

Основные параметры:

- `SWEEP_START = -1.22` — начало дуги удара относительно направления мыши;
- `SWEEP_END = 1.04` — конец дуги;
- `damage = 34.0` — базовый урон;
- `sword_length = 91.0` — видимая длина клинка и внешний радиус попадания;
- `cooldown_duration = 0.36` — пауза между ударами;
- `swing_duration = 0.28` — длительность анимации.

Направление удара чередуется автоматически. `next_swing_direction` хранит направление следующей атаки: первый замах идёт слева направо, второй справа налево. Методы `swing_start_offset()` и `swing_end_offset()` меняют начало и конец анимации местами, не изменяя сектор попадания.

Как изменить размах:

- сделать шире — уменьшить `SWEEP_START` и увеличить `SWEEP_END`;
- сделать уже — приблизить оба значения к нулю;
- сместить удар в одну сторону — одновременно сдвинуть оба значения.

`point_in_sweep()` проверяет цель в экранной изометрической геометрии. Ближняя граница начинается у основания клинка, внешняя заканчивается у наконечника. Размер цели учитывается через `target_radius`.

Для группы `enemy_projectile` к радиусу прибавляется `7.0`: это допуск для надёжного отбивания быстро летящей стрелы. Дальность удара по обычным врагам при этом не меняется.

`_hit_group()` атакует три группы:

- `enemy` вызывает `take_damage()`;
- `enemy_projectile` вызывает `destroy_by_sword()`;
- `destructible` вызывает `hit_by_sword()`.

`upgrade_sword()` увеличивает длину, урон и уровень модели меча. Если меняете стартовые параметры, обновите их также в `reset()`.

## Противники

### `scripts/enemies/enemy_base.gd`

Базовый класс всех врагов. Конкретные типы наследуются от него.

Общие параметры:

- `hit_radius` — размер цели для удара мечом;
- `max_health` и `health` — здоровье;
- `move_speed` — скорость;
- `contact_damage` — урон атаки;
- `experience_value` — выдаваемый опыт;
- `enemy_kind` — строковый идентификатор типа.

`setup()` применяет сложность волны. Здоровье умножается на `difficulty`, скорость и урон растут мягче.

Основные методы:

- `_process()` вызывает индивидуальный `tick_behavior()`;
- `move_toward_player()` реализует стандартное движение к герою;
- `take_damage()` получает урон, применяет отбрасывание и испускает сигнал `died`;
- `damage_player()` передаёт урон герою;
- `draw_health_bar()` рисует полоску повреждённому врагу.

Не помещайте индивидуальную логику конкретного врага сюда, если она не должна применяться ко всем типам.

### `scripts/enemies/brawler_enemy.gd`

Базовый серый квадрат без оружия, атакующий попеременно левым и правым кулаком.

Стартовые характеристики:

- радиус `23`;
- здоровье `64`;
- скорость `76`;
- урон `11`;
- опыт `17`.

Поведение:

- подходит до дистанции `58`;
- запускает удар длительностью `0.42` секунды;
- урон применяется примерно в середине анимации на дистанции до `72`;
- пауза между атаками — `1.45` секунды;
- `punch_side` чередует левую и правую руку.

В `_draw()` серый квадрат рисуется цветом `666662`, а движение активного кулака рассчитывается через `sin(punch_progress * PI)`.

### `scripts/enemies/melee_enemy.gd`

Зелёный квадрат с мечом.

Стартовые характеристики в `_init()`:

- радиус `25`;
- здоровье `82`;
- скорость `78`;
- урон `14`;
- опыт `22`.

Поведение в `tick_behavior()`:

- движется, пока дистанция больше `62`;
- наносит удар на дистанции до `72`;
- пауза между ударами — `1.25` секунды;
- `weapon_phase` слегка двигает меч в ожидании;
- настоящая атака длится `0.38` секунды и поочерёдно идёт слева направо и справа налево;
- `_draw_swing_trail()` рисует зелёный след клинка только во время атаки.

Внешность и цвет меняются в `_draw()`. Основной зелёный цвет — `405d32`.

### `scripts/enemies/shooter_enemy.gd`

Красный квадрат, стреляющий сбиваемыми снарядами.

Стартовые характеристики:

- радиус `22`;
- здоровье `54`;
- скорость `62`;
- контактный урон `10`;
- опыт `26`.

Поведение:

- отступает, если ближе `300`;
- приближается, если дальше `430`;
- постоянно смещается вбок;
- стреляет на дистанции до `620`;
- задержка между выстрелами — `2.35` секунды;
- урон создаваемого снаряда — `12`.

Красный цвет модели меняется в `_draw()`, сейчас это `8f211d`.

### `scripts/enemies/lancer_enemy.gd`

Ромб с копьём.

Стартовые характеристики:

- радиус `27`;
- здоровье `96`;
- скорость `70`;
- урон `18`;
- опыт `30`.

Поведение:

- приближается до дистанции `138`;
- может попасть на дистанции до `158`;
- задержка между выпадами — `2.05` секунды;
- визуальная длительность выпада — `0.32` секунды.

Длина копья в `_draw()` равна `65` в спокойном состоянии и `95` во время выпада.

### `scripts/enemies/boss_enemy.gd`

Большой квадрат с топором.

Стартовые характеристики:

- радиус `58`;
- здоровье `920`;
- скорость `34`;
- урон `36`;
- опыт `300`.

Поведение:

- подходит до дистанции `142`;
- начинает большой замах продолжительностью `1.4` секунды;
- наносит урон в конце замаха на дистанции до `185`;
- отдыхает между замахами `4.5` секунды.

Модель топора и положение во время замаха находятся в `_draw()`.

## Снаряды

### `scripts/combat/enemy_projectile.gd`

Небольшая красная стрела, выпускаемая стрелком. Направление модели рассчитывается из `velocity`, поэтому наконечник всегда смотрит по ходу полёта.

Параметры:

- `damage = 12.0` — значение по умолчанию, но стрелок передаёт своё при создании;
- `hit_radius = 14.0` — размер столкновения и базовая область попадания мечом;
- `lifetime = 6.0` — время до автоматического удаления;
- `280.0` в `setup()` — скорость полёта.

`destroy_by_sword()` удаляет стрелу, когда она попала в сектор меча. В `PlayerAttack` для стрел дополнительно применяется допуск `7.0`, поэтому их можно уверенно отбивать краем клинка. Модель древка, наконечника и оперения находится в `_draw()`.

## Локация

### `scripts/world/location.gd`

Создаёт пол, границы и случайные объекты.

Экспортируемые параметры:

- `world_limit = 3600.0` — карта от `-3600` до `3600` по каждой оси;
- `tile_size = 180.0` — размер плиток.

`regenerate()` очищает старые объекты и создаёт новые. Текущий состав:

```gdscript
_generate_kind("bush", 120)
_generate_kind("barrel", 64)
_generate_kind("tree", 46)
```

Меняйте второе число для изменения количества. В `_generate_kind()`:

- `310.0` — свободная область вокруг старта героя;
- `82.0` — минимальное расстояние между объектами;
- `170.0` — отступ от края карты.

`_draw()` строит двухцветный изометрический пол и золотую границу. Цвета плиток — `171310` и `211913`.

Размер карты должен совпадать с `world_limit` в `player.gd`. Если меняете карту через Inspector сцены локации, измените аналогичный параметр сцены героя.

### `scripts/world/world_prop.gd`

Описывает куст, бочку и дерево.

`setup()` выбирает тип:

- `tree` — радиус `44`, неразрушаемый;
- `barrel` — радиус `25`, разрушаемый;
- `bush` — радиус `30`, разрушаемый.

`hit_by_sword()` удаляет разрушаемый объект и может запросить бутылку:

- бочка — шанс `0.42`;
- куст — шанс `0.20`.

`_draw()` содержит отдельные ветки отрисовки каждого объекта. Для нового вида добавьте его параметры в `setup()`, новую ветку в `_draw()` и вызов генерации в `location.gd`.

## Предметы

### `scripts/items/pickup.gd`

Один класс для опыта и лечебной бутылки. Тип задаётся строкой `pickup_kind`.

Опыт:

- начинает магнититься на дистанции `player.attack.sword_length + 42.0`;
- скорость притяжения начинается с `220.0` и растёт рядом с героем;
- при подборе вызывает `player.add_experience(value)`.

Бутылка:

- не магнитится;
- при подборе вызывает `player.heal(value)`;
- сила лечения передаётся из `_spawn_potion()` файла `main.gd`.

Размер подбора задаёт `hit_radius = 10.0`. Внешний вид обоих предметов находится в `_draw()`.

## Волны

### `scripts/systems/wave_manager.gd`

Управляет временем, частотой появления и составом волн.

- `wave_duration = 18.0` — продолжительность одной волны;
- `spawn_time` — время до следующего врага;
- `spawn_index` — позиция в гарантированном списке типов;
- после волны `7` испускается `boss_requested`.

Частота появления рассчитывается так:

```gdscript
maxf(0.42, 1.35 - float(wave) * 0.11)
```

Первое число `0.42` — минимальная возможная задержка. `1.35` — базовая задержка. `0.11` — ускорение за каждую волну.

Состав задаётся массивами в `_choose_enemy()`:

- волна 1 — серые бойцы, мечники и стрелки;
- волна 2 — добавляются копейщики;
- волны 3–4 — используются все четыре обычных типа;
- волны 5–7 — серые бойцы остаются базовыми, а мечники, стрелки и копейщики чередуются с ними.

Строка в массиве должна совпадать с веткой `match` в `main.gd`.

Сложность врага передаётся формулой `1.0 + (wave - 1) * 0.13`. Увеличьте `0.13`, чтобы здоровье и урон врагов росли быстрее.

## Интерфейс

### `scripts/ui/game_ui.gd`

Создаёт интерфейс кодом, без отдельных `.tscn`-панелей.

- `_build_menu()` — главное меню, заголовок и кнопка старта;
- `_build_hud()` — здоровье, опыт, волна и уровень;
- `_build_upgrade_panel()` — три варианта усилений;
- `_build_game_over_panel()` — победа или поражение;
- `_button()` — общие параметры всех кнопок;
- `_panel_color()` — фоновый цвет панелей;
- `set_health()`, `set_experience()`, `set_wave()` — обновление HUD.

Для изменения положения элемента меняйте `position` и `size`. Для текста — свойство `text`. Размер шрифта задаётся через `add_theme_font_size_override()`.

Если интерфейс станет сложнее, рекомендуется вынести его в отдельную сцену `scenes/ui/game_ui.tscn`, а в этом скрипте оставить только обновление данных.

## Служебные `.uid`-файлы

Рядом с каждым `.gd` Godot создаёт файл вида `player.gd.uid`.

Они нужны движку для устойчивых ссылок на ресурсы. Их следует хранить в Git, но не нужно открывать или редактировать вручную. Если удалить `.uid`, Godot создаст новый, из-за чего ссылки в сценах могут измениться.

## Как добавить нового противника

Пример: новый быстрый фиолетовый враг.

1. Скопируйте `scripts/enemies/melee_enemy.gd` в `scripts/enemies/fast_enemy.gd`.
2. Поменяйте `class_name` на `FastEnemy`.
3. В `_init()` задайте уникальный `enemy_kind = "fast"` и характеристики.
4. Измените `tick_behavior()` для нового поведения.
5. Измените `_draw()` для другой модели и цвета.
6. Скопируйте сцену `scenes/enemies/melee_enemy.tscn` в `fast_enemy.tscn`.
7. Подключите в новой сцене `fast_enemy.gd`.
8. В `scripts/main.gd` добавьте:

```gdscript
const FAST_ENEMY_SCENE := preload("res://scenes/enemies/fast_enemy.tscn")
```

9. В `match enemy_kind` добавьте:

```gdscript
"fast":
	 enemy = FAST_ENEMY_SCENE.instantiate() as EnemyBase
```

10. Добавьте строку `"fast"` в нужный массив файла `wave_manager.gd`.
11. Запустите проверку Godot и убедитесь, что новый тип появляется.

## Как добавить новое улучшение уровня

1. Добавьте кнопку в `_build_upgrade_panel()` файла `game_ui.gd`.
2. Передайте новый строковый идентификатор через `upgrade_selected`.
3. Добавьте ветку в `_apply_upgrade()` файла `main.gd`.
4. Реализуйте отдельный метод изменения параметров в том модуле, которому принадлежит улучшение.

Например, усиление скорости снарядов противника должно находиться в системе противников или снарядов, а не в UI.

## Как добавить новый объект карты

1. Добавьте строковый тип в `WorldProp.setup()`.
2. Укажите радиус и разрушаемость.
3. Добавьте ветку отрисовки в `WorldProp._draw()`.
4. При необходимости добавьте выпадение предмета в `hit_by_sword()`.
5. В `GameLocation.regenerate()` вызовите `_generate_kind("новый_тип", количество)`.

## Проверка после изменений

В Godot:

1. Откройте проект и дождитесь завершения импорта.
2. Проверьте панель **Output** на ошибки `Parse Error` и `Invalid call`.
3. Запустите главную сцену.
4. Начните забег и проверьте изменённую механику.

Безоконная проверка из терминала:

```powershell
Godot_v4.7.1-stable_win64.exe --headless --editor --path . --quit
Godot_v4.7.1-stable_win64.exe --headless --path . --quit-after 3
```

Первая команда компилирует и импортирует проект. Вторая кратко запускает главную сцену и помогает найти ошибки времени выполнения.

## Безопасная работа с Git

Перед изменениями убедитесь, что выбрана ветка `godot-port`.

Рекомендуемый порядок:

```text
1. Изменить один модуль.
2. Запустить проект.
3. Проверить связанную механику.
4. Сделать отдельный коммит с понятным названием.
```

Не добавляйте в Git папку `.godot/`: это локальный кэш. Не удаляйте `.gd.uid`. Браузерную версию изменяйте отдельно в ветке `circle-raider`.
